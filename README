how to get started with the virtual environment: 

install venv if you don't have it. navigate to the project directory:
$ pip install venv
$ python3 -m fygs
Activate the virtual environment:
$ source fygs/bin/activate
Get the packages in requirements.txt:
$ pip install -r requirements.txt

If you want to add to requirements.txt:
$ pip freeze > requirements.txt